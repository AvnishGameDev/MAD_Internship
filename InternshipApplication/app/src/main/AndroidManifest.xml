<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InternshipApplication"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.InternshipApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".CheckboxRadioButtonActivity" />
        <activity android:name=".PizzaOrderCustomizerActivity" />
        <activity android:name=".ShareContactActivity" />
        <activity android:name=".ConfirmationActivity" />
        <activity android:name=".TemperatureConvertorActivity" />
        <activity android:name=".CalculatorActivity" />
        <activity android:name=".UTSActivity" />
        <activity android:name=".CoinFlipActivity" />
        <activity android:name=".TempActivity" />
        <activity android:name=".DiceRollActivity" />
        <activity android:name=".FlipkartActivity" />
        <activity android:name=".ShoppingListActivity" />
        <activity android:name=".CollegeListActivity" />
        <activity android:name=".NotesMainActivity" />
        <activity android:name=".NoteAddActivity" />
        <activity android:name=".AdvancedShoppingListActivity" />
    </application>

</manifest>